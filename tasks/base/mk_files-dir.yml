---
- name: make files dir
  file:
    path: "files/{{ ocp.version }}"
    state: directory

- name: make files/clients dir
  file:
    path: "files/{{ ocp.version }}/clients"
    state: directory

- name: make files/dependencies dir
  file:
    path: "files/{{ ocp.version }}/dependencies"
    state: directory

- name: make files/conf dir
  file:
    path: "files/{{ ocp.version }}/conf"
    state: directory
